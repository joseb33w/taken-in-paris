# TAKEN IN PARIS — backend config (Supabase, shared Gogi project).
# These values are baked into web/bridge.js at export time. The next session
# (cloud or in-chat) reads VITE_TABLE_PREFIX from here to locate this app's tables.

# Per-APP table namespace. Every table is "<VITE_TABLE_PREFIX>_<name>".
VITE_TABLE_PREFIX=usr_nmexs7bytxq2_taken_in_paris

# Shared Gogi Supabase project (anon/publishable key is safe in the client).
VITE_SUPABASE_URL=https://xhhmxabftbyxrirvvihn.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxx_set_at_export_time
